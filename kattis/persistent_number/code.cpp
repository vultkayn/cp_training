#include <iostream>
#include <vector>
using namespace std;
using vvi = vector<vector<long>>;


// decomposer en les nombre premiers {2,3,7}
// si un nombre premier supérieur ou égal à 11 est dans la décomposition du nombre, ecrire There is no such number
// ---> (il suffit de diviser successivement par 2,3,7, si on ne peut plus et que le résultat n'est pas 1, alors on est foutu.)
// sinon packer la décomposition en digits les plus possibles, par exemple 2x2x2 en 8, et 2x3 en 6
// ordonner ensuite les digits obtenu par ordre décroissant, cela donne la réponse



int main(){

    return 0;
}
